@page "/R_ProductosEmpaque"

<EditForm Model="productoEmpaque" OnValidSubmit="Guardar">
    <DataAnnotationsValidator />
    <ValidationSummary />

    <div class="container-sm row-cols-md-1">

        <div class="card shadow-lg">

            <div class="card-header">
                <h3 class="card-title">Registro de Empaques</h3>
            </div>

            <div class="card-body">

                @* Buscar *@
                <div class="form-group align-items-right">
                    <div class="form-row">
                        <label>Producto Id: </label>
                        <div class="input-group">

                            <InputNumber @bind-Value="productoEmpaque.ProductoId" class="form-control col-4 col" />

                            <div class="input-group-append col">

                                <button type="button" class="btn btn-success input-group-text" @onclick="Buscar">
                                    <span class="oi oi-magnifying-glass"> Buscar</span>
                                </button>
                            </div>
                        </div>
                    </div>
                </div>

                <hr>

                @* Fecha *@
                <div class="row">
                    <div class="form-group col">
                        <label>Fecha de Expiracion: </label>
                        <InputDate class="form-control" @bind-Value="productoEmpaque.Fecha" />
                        <ValidationMessage For="@(() => productoEmpaque.Fecha)" />
                    </div>
                </div>

                <br>

                @* Concepto *@
                <div class="row">
                    <div class="form-group col">
                        <label>Concepto: </label>
                        <InputText class="form-control" @bind-Value="productoEmpaque.Concepto"
                            placeholder="Descripción del producto" />
                        <ValidationMessage For="@(() => productoEmpaque.Concepto)" />
                    </div>
                </div>

                <hr>
                <br>

                @* Utilizados *@
                <div class="card">

                    <div class="card-header">
                        <h3>Utilizados</h3>
                    </div>

                    <form>

                        <div class="row align-items-end">

                            <div class="form-group col">
                                <label>Productos: </label>
                                <select class="form-select col" aria-label="Default select example">
                                    <option selected>Productos</option>

                                    <option value="1">One</option>
                                    <option value="2">Two</option>
                                    <option value="3">Three</option>
                                </select>
                            </div>

                            @* Cantidad *@
                            <div class="form-group col">
                                <label>Cantidad: </label>
                                <InputNumber class="form-control" @bind-Value="productoEmpaque.CantidadUtilizados" />
                                <ValidationMessage For="@(() => productoEmpaque.CantidadUtilizados)" />
                            </div>

                            <div class="form-group col">
                                <button type="button" class="btn btn-success col" @onclick="AgregarUtilizados">
                                    <span class="oi oi-plus"> Agregar</span>
                                </button>
                            </div>

                        </div>

                        <br>

                        <table class="table table-secondary">
                            <thead class="thead-dark">
                                <tr class="table-info">
                                    <th>ProductoId</th>
                                    <th>Descripcion</th>
                                    <th>Cantidad</th>
                                </tr>
                            </thead>
                            <tbody>
                                @* @foreach (var item in producto)
                                    {
                                    <tr>
                                    <td>@item.Id</td>
                                    <td>@item.Concepto</td>
                                    <td>@item.Cantidad</td>
                                    <td><span class="btn btn-sm btn-outline-primary oi oi-ban" href="#" @onclick=" (() =>
                                    RemoverFila(item)) "> Remover</span></td>
                                    </tr>
                                    } *@
                            </tbody>
                        </table>
                    </form>

                    @* Total *@
                    <div class="input-group mb-3">
                        <span class="input-group-text" id="basic-addon1">
                            Peso Total: 
                        </span>
                        <input type="text" class="form-control" readonly>
                    </div>

                </div>

                <hr>
                <br>

                @* Producidos *@
                <div class="card">

                    <div class="card-header">
                        <h3>Producido</h3>
                    </div>

                    <br>

                    <form>

                        <div class="row">

                            @* Combo BOX Productos *@
                            <div class="form-group col">
                                <label>Productos: </label>
                                <select class="form-select col">
                                    <option selected>Productos</option>
                                    <option value="1">One</option>
                                    <option value="2">Two</option>
                                    <option value="3">Three</option>
                                </select>
                            </div>

                            @* Cantidad *@
                            <div class="form-group col">
                                <label>Cantidad: </label>
                                <InputNumber class="form-control" @bind-Value="productoEmpaque.CantidadPro" />
                                <ValidationMessage For="@(() => productoEmpaque.CantidadProducido)" />
                            </div>

                        </div>

                    </form>

                </div>
            </div>

            <div class="card-footer">

                @* Botones *@
                <div class="form-group text-center" display: inline-block>

                    <button type="button" class="btn btn-lg btn-primary" @onclick="Nuevo">
                        <span class="oi oi-plus"> Nuevo</span>
                    </button>

                    <button type="submit" class="btn btn-lg btn-success">
                        <span class="oi oi-folder"> Guardar</span>
                    </button>

                    <button type="button" class="btn btn-lg btn-danger" @onclick="Eliminar">
                        <span class="oi oi-trash"> Eliminar</span>
                    </button>

                </div>

            </div>

        </div>
    </div>

</EditForm>

@inject IToastService toastService

@code {

    public int indice { get; set; } = 0;
    public ProductosEmpaque productoEmpaque { get; set; } = new ProductosEmpaque();

    [Inject]
    public ProductoBLL productosBLL { get; set; }
    
    [Inject]
    public ProductoEmpaqueBLL productosEmpaqueBLL { get; set; }

    protected override void OnInitialized()
    {
        
    }

    public void Buscar()
    {
        
    }

    public void AgregarUtilizados()
    {

    }

    public void Nuevo()
    {
        
    }

    public void Guardar()
    {
        
    }
    public void Eliminar()
    {
        
    }


}
